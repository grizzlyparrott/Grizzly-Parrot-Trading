# Independent editorial distinctiveness proofread: ES/MES and NQ/MNQ library

Review date: 2026-08-28

Reviewer role: independent editorial distinctiveness/cannibalization proofreader. No article, script, test, hub, search-index, sitemap, or stylesheet was edited during this review or recheck.

Scope: all 35 pages in `scripts/equity_index_cluster_config.py`, their complete visible article bodies and disclosures, `artifacts/equity-index-cluster-classification.md`, the current working-tree diff, the sameness audit, fail-closed validator, and relevant non-mutating tests.

## Final fail-closed verdict

**PASS — 0 unresolved findings, 0 audit errors, and 0 audit warnings.**

The two initial findings were corrected and independently rechecked. The ES/MES comparison now uses an existing shared component with no inline CSS; validator and regression coverage now fail on any `style=` attribute; and all five previously templated research disclosures now state genuinely page-specific evidence boundaries. No correction introduced a new purpose collision, copied component sequence, repeated disclosure, or stylesheet exception.

Every URL fulfills its exclusive reader task. Neighboring pages complement one another rather than targeting the same query with the same answer. Openings, H2 sequences, examples, inner component sequences, decision artifacts, substantive endings, and page-specific disclosure language are distinct. Empirical pages label unperformed work as protocols rather than findings, comparison pages retain their assigned axes, and the two canonical mechanics pages remain the specification authorities.

## Recheck method

1. Reread the corrected ES/MES comparison component and all five corrected disclosure blocks in source context.
2. Rescanned the complete 35-page tuple for `<style>` blocks, `style=` attributes, stylesheet URLs, hero ledes, H2 sequences, ending components, disclosure similarity, and canonical mechanics routing.
3. Reran the full 35-page distinctiveness audit and fail-closed validator from the working tree.
4. Reran 15 non-mutating equity-index correctness/release and shared-theme tests.
5. Reinspected the current diff and confirmed that `futures-basics/currency-research-library.css` remains unmodified.

## Resolution of initial findings

### 1. RESOLVED — inline CSS on the ES/MES comparison

`futures-basics/es-mini-vs-mes-micro-which-should-you-trade.html:31` now expresses the $400 ES versus $40 MES illustration with `fx-stat-grid` and two `fx-stat` blocks. The previous `style="width:100%"` and `style="width:10%"` attributes are gone.

Independent cluster scan result:

- 35/35 pages load exactly `../style.css` and `/futures-basics/currency-research-library.css?v=20260820a`.
- 35/35 pages contain zero `<style>` blocks.
- 35/35 pages contain zero `style=` attributes.
- The canonical shared stylesheet has no working-tree diff.

The earlier validation blind spot is also closed. `scripts/validate_equity_index_cluster.py:284-285` rejects any whitespace-delimited `style=` attribute case-insensitively, and `tests/test_equity_index_cluster_release.py:58` enforces the same cluster-wide rule. The current validator and regression test pass with the repaired markup.

### 2. RESOLVED — noun-substitution research disclosures

The five corrected disclosures now disclose different page-specific limits:

- `nq-best-times.html:109` says the page publishes a falsifiable time-window study plan and reports neither measured performance, an optimal window, nor an edge.
- `nq-liquidity-windows.html:100` says the metric stack awaits measured data and that its thresholds and expiry rules are not empirical results.
- `nq-pullbacks-vs-breakouts.html:119` says the entry/matching rules define a future comparison and report no pullback or breakout advantage.
- `nq-volatility-vs-es.html:111` says the regime decomposition is a measurement specification, not evidence of predictably greater volatility or tradability.
- `es-session-highs-lows-and-vwap-usage.html:104` separates the conventional VWAP formula from test specifications and reports no tested level reliability or edge.

These are not synonym substitutions inside one sentence frame. They separately identify the study status, missing evidence, and claim that each page does not make. A fresh independent pairwise scan no longer places any of these five among the highest disclosure similarities. The former 0.852/0.837/0.831 clone group is gone; the current maximum is 0.7742 between the NQ/ES portfolio comparison and the NQ orientation page. Manual review confirms that pair is legitimate publication framing: one disclaims an empirical instrument winner, while the other disclaims product-performance findings and inferred participant motive.

## Current automated and structural evidence

- `py scripts/audit_equity_index_distinctiveness.py` — **35 pages, 35 unique component signatures, 0 errors, 0 warnings**.
- `py scripts/validate_equity_index_cluster.py --warnings-as-errors` — **35 pages, 0 errors, 0 warnings**.
- Selected equity-index release/correctness tests plus `tests.test_currency_library_theme` — **15 passed**.
- `git diff --check` — exit 0; only Git's LF-to-CRLF working-copy notices were printed.
- The current JSON report agrees: 35 pages, 35 unique signatures, no errors, and no warnings.
- Maximum audit values remain below their gates:
  - H2-sequence similarity: **0.1257**.
  - Eight-word lexical Jaccard overlap: **0.0112**.
  - Meta-description similarity: **0.6250**.
  - Structural-component sequence similarity: **0.7368**.
- Independent current-file scan found **35 unique hero ledes, 35 unique H2 sequences, and 35 unique final substantive components**.
- No exact substantive paragraph, hero lede, non-generic normalized H2, disclosure sentence, or 18-word disclosure shingle is duplicated across pages.

The one release-test method that invokes the audit with `--json artifacts/equity-index-distinctiveness-report.json` was not rerun by this proofreader because this role's only permitted write is this report. The underlying audit was run directly without `--json`; the existing JSON report had already been regenerated in the current working tree and matches the direct result.

## Concise five-page human sample

| Page | Human distinctiveness check | Verdict |
|---|---|---|
| `best-times-to-trade-es-e-mini-sp500.html` | Opens with one clock time containing incompatible book/event states; maps UTC sessions, task-specific market-quality measures, exclusions, ranking stability, and personal window review. It reports no winning clock. | PASS |
| `es-mini-vs-mes-micro-which-should-you-trade.html` | Owns same-index multiplier fit, whole-contract granularity, cost-per-exposure, and zero/ES/MES selection. The repaired stat grid preserves the worked $400/$40 contrast without page CSS. | PASS |
| `es-using-dom-and-time-and-sales.html` | Owns what DOM, tape, and order-state data can prove; synchronizes their clocks, tests rival explanations, limits spoofing attribution, and ends in a replay record. It does not duplicate historical liquidity-pocket measurement. | PASS |
| `nq-vs-es.html` | Opens two different index portfolios, compares eligibility and effective weights, normalizes contract risk, and chooses by exposure fidelity plus executable loss. It routes empirical volatility to the separate protocol. | PASS |
| `why-futures-lead-the-stock-market.html` | Owns conditional cross-venue price discovery, active-market clocks, arbitrage transmission, historical lead/lag evidence, counterexamples, and synchronized testing. It does not collapse into the practical ES/SPY/SPX confirmation page. | PASS |

## Full-cluster anti-cannibalization evidence

| Neighboring family | Exclusive ownership verified | Result |
|---|---|---|
| ES/NQ best-times pages versus NQ liquidity windows | Broad task-fit clock ranking versus live state construction, persistence, order-size permission, and expiry. | PASS |
| ES liquidity pockets versus ES DOM/time-and-sales | Historical/descriptive book reconstruction and conditional distributions versus live observation layers, attribution limits, order gates, and replay review. | PASS |
| ES economic reports versus ES news/volatility traps | Macro surprise and competing valuation channels versus event taxonomy, impaired fills, halts/limits, and operational branches. | PASS |
| ES gap behavior versus ES opening range | Close/open reference definitions and gap-path testing versus opening-range availability, strategy variants, and holdout protocol. | PASS |
| ES plan versus ES sizing versus ES mistakes | Permission/version control versus whole-contract risk arithmetic versus incident-chain detection, containment, and repair. | PASS |
| ES/MES comparison versus ES/MES/NQ selector versus NQ/ES portfolio comparison versus NQ/ES volatility | Same-index multiplier fit; three-contract implementation choice; index-exposure decomposition; normalized empirical volatility protocol. | PASS |
| NQ orientation versus NQ mechanics versus NQ margin | Index/contract identity and learning path; canonical ticket/arithmetic/lifecycle record; changing collateral and cash-buffer workflow. | PASS |
| NQ earnings versus NQ news volatility | Constituent weight/direct contribution and peer read-through versus broad catalyst families, valuation channels, and liquidity amplification. | PASS |
| Market-wide futures leadership versus ES/SPY/SPX confirmation | Conditional venue price discovery and lead/lag evidence versus practical identity, clock, basis, and price-type alignment. | PASS |

## Final release recommendation

**Unconditional PASS.** No actionable distinctiveness, cannibalization, templating, research-status, mechanics-ownership, component-architecture, or shared-stylesheet finding remains in the 35-page scope. This proofreader does not block release.
