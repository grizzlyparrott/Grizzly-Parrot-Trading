# Local credentials must never be committed. The production wrangler.toml
# contains only non-secret bindings and keeps the sales gate explicitly false.
.dev.vars
.env
.env.*
!.env.example

node_modules/
.wrangler/
.dist/
